# ML tests

- `test_ml_accuracy.py` — checks that each trained ML model meets the accuracy thresholds documented in the CropSphere Dataset and Model Report. Runs directly against model files and CSV data, not through the FastAPI app.
